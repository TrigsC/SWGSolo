# Coverage Debt Ledger

One line per uncovered risky path: `path | why hard | escape plan`. Delete a
line in the same change that gives its path meaningful coverage.

- `SimPlayerManager::planPvpRoute` tactical-arrival block + `isPvpCityHotLocked` | interwoven with `pvpSquadMutex` state and live Zone/PlanetManager lookups; no SimPvP gtest scaffolding exists | live-dashboard verification per `docs/ai-pvp-mimetic-travel-design.md` §13.3 (tacticalArrivalsTotal > 0, convergence rows stay direct)
- `SimPlayerController` cell-aware path pipeline (in-cell `PatrolPoint`/arrival math) | requires a zone + building cells + pathfinding fixture; DeadlockTestBase does not cover movement | live visual check at Theed Spaceport per §13.3; miners regression-checked via minerActivity baseline
- `SimPlayerManager::checkPvpBreakOff` rolling window + latch/promotion interplay | interwoven with pvpSquadMutex squad state, controller map, and live death callbacks | staged-gank live verification per design doc §14.2 (incl. leader-as-threshold-death case)
- `SimPvpBotController::onTick` stalemate progress tracker + re-engage suppression | per-tick combat-state machine entangled with live agent references and manager config; no SimPvP gtest scaffolding | live verification per `docs/ai-pvp-mimetic-travel-design.md` §15.2 (stalemateBreaksTotal climbing at idleMs≈45s, hardStuck clearCombat rate collapse)
- `SimHunterController` hunt state machine (disengage-move-reengage combat, retreat/heal, clone-wound, kill-observer harvest) | per-tick combat/movement machine over live agents + spawned creatures + ResourceManager + pveMutex; no SimPve gtest scaffolding | live verification per design doc §Phase 2 (hunter travels→engages→KILLS→typed harvest; retreat + death/clone observed)
- `SimHunterController` P.8.2 mission-terminal phases and lair lifecycle handoff (terminal pending/absent fallback, acceptance dwell, lair travel, social-add retreat/abandon, cleanup) plus `SimPlayerManager` mission dashboard snapshots | live controller state, real `CityRegion` terminals, non-persistent `LairObject` records, queued destruction, and pveMutex; no SimPve gtest scaffolding | live verification per `docs/ai-pve-playerbot-design.md` §P.8.2 (terminal→lair→local pull→kill→cleanup; pending/absent fallback and add-cap abandonment)
- `SimPlayerManager` PvE acquisition-ledger math (P.8.1c/P.8.1d): `deriveDemandFamilySignals` two-component allocation (alloc capped by stable ceiling headroom + largest-remainder ≤ shared gap; independent capped reserve floor), `preparePveCreatureFamilySupply` boot-baseline state machine (pending→capturing→ready + failure/backoff) with hunter-origin filtering, composite-origin yield accumulation/flush, miner creature-resource exclusion, `computeReservedInboundByProfileFamily` single-pveMutex reservation+session snapshot, and `runPveHunterMatchmaker` value/time ranking with intra-pass saturating reservation | file-local static helpers threaded through the ~19k-line demand engine + live AiEconomyManager persistence + pveMutex; unit-testing needs internal exposure and a persistence/stockpile fixture that no SimPve/demand gtest scaffold provides | live-dashboard verification via `pveActivity.demandFamilies` (hunter-origin meat signal > 0 → allocComponent shrinks as familySupply rises → 0 at ceiling; `bootBaselineHunterMeat` confirms miner meat is discounted; reservedInboundSupply nets same-pass; baselineState=ready) + hunters leaving IDLE_HOME and no new miner creature-resource lots, per docs/ai-pve-playerbot-design.md §P.8.1d
- `SimPlayerManager::announcePveHunterEvent` detail/site mapping + `SimHunterController::applyHunterBuffs` HAM fill/refresh | requires live ChatManager, agent Buff state, and timing across controller phases; no SimPve unit fixture | in-game phase announcements and HAM-pool observation, with `pveActivity.hunterAnnouncementsTotal` and the existing live hunter loop
- `SimPlayerController` hunter hybrid movement mode and `#/wilds` roster rendering | navmesh/zone geometry and browser DOM are external to the current unit-test scaffolding; widget is a static whitelisted SPA | live city/wilderness movement observation plus dashboard `#/wilds` position/phase/`/way` check; run the existing frontend smoke hook with a representative `pveActivity.roster[]` snapshot when browser tooling is available
- `SimHunterController` P.8.4 combat-target selection (`selectActiveCombatAttacker` nearest-first + followObject hysteresis, `engageActiveAttacker` species-vs-interceptor dispatch, `shedAllDefendersBilaterally`/`resetInterceptorCombat` bilateral resets, conditional stalemate) and `CreatureObjectImplementation::isAttackableBy` neutral-sim-bot-vs-player guard | predicate/state machine over live defender lists, `@preLocked` combat mutations, faction/ghost/no-combat-area chains, and pveMutex-adjacent controller state; the `isAttackableBy` sim-bot branch needs a full player `PlayerObject`/ghost + AiAgent faction fixture that dwarfs the assertion (mock-pain tripwire), and no SimPve gtest scaffold covers the controller | live/in-game verification per `docs/ai-pve-playerbot-design.md` §P.8.4 (Bug 1: neutral player takes NO grenade-AoE damage and is never targeted/locked; Bug 2: roster `targetOid` re-points to the actual attacker within ~1 tick with no no-shot stall; `hunterKillsTotal` keeps climbing on wildlife only)
- `SimHunterController` P.8.6 real-buff need detection + provider flow (`computeBuffNeeds` tracked-CRC/threshold split, `moveToNextPveBuffProvider` stage machine, `interactWithPveBuffProvider` watch/listen + doctor `ScreenPlayTask` + poll/timeout/cancel, `applyHunterBuffsForFamily` fallback) and `AiAgentImplementation::healEnhanceCreatureTarget` refresh-in-place + `smartDoctorBuffer.lua` sim-bot token/auto-confirm/no-strip | per-tick controller machine over live `AiAgent`/`Buff` state, `PlayerManager::startWatch/startListen`, thread-local screenplay Lua states, and pveMutex; the buff-need + healEnhance-refresh paths need a full `CreatureObject`+`Buff`+`AiAgent` fixture that dwarfs the assertion (mock-pain tripwire), and no SimPve gtest scaffold covers the controller | live-dashboard verification per `docs/ai-pve-playerbot-design.md` §P.8.6 (`realBuffs.enabled=true` restart: `medicalBuffed`/`entertainerBuffed` flip true with `lastBuffSource=real-doctor`/`real-entertainer`; interrupted doctor session leaves processed medical pools fresh + unprocessed pools untouched; fresh-buff hunters skip the detour = `buffDetoursSkipped` climbs; Bestine hunters use `syntheticFallbacks`; `hunterKillsTotal` keeps climbing)
- `SimPlayerManager` P.8.7 mission-board + market-dispatch pure helpers (`pveMarketQualityScore` weighted-mean with absent-stat drop and all-absent skip, `pveMarketTieEarlier` four-level deterministic tie-break, `selectPveBotMissionLairSpawn` three-tier level-window relaxation mirroring `getRandomLairSpawn`, and the city-centre→terminal bearing/arc constraint for same-direction offers) | these are genuinely pure and *worth* testing — the blocker is purely structural: they are file-local `static` functions and private members buried in the ~31k-line `SimPlayerManager.cpp`, with no SimPve gtest scaffold and no seam to link against | ESCAPE PLAN (cheap, do this next slice): extract the four helpers into a small header-only `SimPveMissionMath.h` taking plain values (stat map + weights; candidate tuples; `LairSpawn` difficulty pairs; two `Vector3`s + arc degrees) so they link into `core3tests` with no fixture at all. Until then: live-dashboard verification per `docs/ai-pve-playerbot-design.md` §P.8.7 (`marketDispatch` picks a deterministic planet/city tuple across restarts given a stable spawn set; `missionBoard.offers[]` shows two offers whose bearings are within `sameDirectionArcDegrees` of the terminal bearing; offered lair difficulty tracks hunter level)
- `SimHunterController` P.8.7 board/relocation state machine (`ENGAGING_LAIR ⇄ HUNTING` wave loop, `hasMissionWaveMobInRange` home-object ownership predicate, 120m reveal trigger, `canBeginInterplanetaryTravel` lair/defender gating, buff-hub round trip) and `SimPlayerManager` lair construction + completion observer | per-tick controller machine over live `AiAgent`s, real `LairObject`s with `DestroyMissionLairObserver` waves, cross-zone `switchZone` travel, and pveMutex; no SimPve gtest scaffold, and a lair-wave fixture would dwarf the assertions (mock-pain tripwire) | live verification per `docs/ai-pve-playerbot-design.md` §P.8.7 (mission completes on LAIR DESTRUCTION not first kill; three waves observed in-world with the "Transmission Received" reveal; `lairsDestroyedTotal`/`missionsCompletedTotal` climb while `abandonReasons` stays small; a relocation never starts with a live lair or defender)
- `SimPlayerManager` F_0.6.0 multi-family hunter demand: multi-family harvest crediting in `recordPveHunterHarvest` (per-family hide/bone/meat type+max resolution, `max < 3` skip, three provenance-tagged accumulator deposits, per-family `pveSessionHarvestByFamily`/order/identity counters), `decrementPveHuntFamilySignals` intra-pass all-three-family signal suppression across every matching profile, and the reserve-all-three rewrite of `computeReservedInboundByProfileFamily` (per-family remaining reserved against every enabled profile that recognizes the family) | the helpers (`getPveCreatureFamily`, `getPveHuntOrder{Expected,Harvested}FamilyUnits`, `decrementPveHuntFamilySignals`) are file-local `static` functions and the harvest path needs live `AiAgent`/`CreatureTemplate`/`Creature`/`ResourceManager`/`ResourceSpawn` + spawnYieldAccumulatorMutex + pveMutex; no SimPve gtest scaffold and a corpse+resource fixture dwarfs the assertions (mock-pain tripwire) | live-dashboard verification per `docs/ai-pve-playerbot-design.md` §F_0.6.0 (`pveActivity.demandFamilies` shows non-zero `hide`+`bone` signals; `harvestByFamily` climbs for all three families; hunter orders dispatch with mixed `harvestKind`; with meat saturated the roster keeps hunting hide/bone instead of all-IDLE_HOME; no same-pass over-dispatch thrash). ESCAPE PLAN: fold into the P.8.7 `SimPveMissionMath.h` extraction — the three family-index/order helpers are pure value functions and would link into `core3tests` with no fixture.
- `SimPvpBotController` P.6.6 controller-driven engagement (`currentWeaponEngageRange` weapon-max-range read + `arrivalTolerance` floor + null-weapon fallback, `driveCombatMovement` approach/engage/LOS/hysteresis/radius-timeout decision split, `hasCombatLineOfSight` cell dispatch, `swapCombatAiMap` setCustomAiMap+setAITemplate with formationOffset preserve/restore, `teardownControllerEngagement` route-resume, and `SimPvpMemberController::assertFollow` combat-latch early-out) | per-tick combat/movement machine over live `AiAgent`/`WeaponObject`/`CollisionManager` + AI-map/blackboard rebuilds + the manager singleton; the range/decision helpers are the pure-worthy parts but read a live equipped weapon and manager config, and a full agent+weapon+cell fixture dwarfs the assertions (mock-pain tripwire); no SimPvP gtest scaffold covers the controller | live-dashboard verification per `docs/ai-pvp-squad-design.md` §P.6.6 (`pvpConfig.combat.controllerDrivenEngage=true` restart: `pvpActivity.combatEngagement` — ranged squads hold ~weapon-max-range apart with no melee stacking, `losBlockedAttacks` only transient, `inCellEngagements` climbs at indoor collectors with zero clip-through via `/v1/object/{oid}`, converging squads produce a clean "paths meet → fight", and `starportTraversal` shows full squads traversing interiors like scouts; churn ≤ P.6.5e baseline)
- `SimPlayerManager::getPvpSquadSharedCombatTargets` (P.6.6b squad aggro-sharing) pruning/liveness/exclude filtering + `recordPvpSquadCombatTarget` TTL insert/refresh/cap, and `SimPvpBotController` convergence acquisition (converge-radius outer bound, converge-specific timeout, failed-target ignore window, prefer-own-closer-target) | the manager helpers resolve+lock live agents via ZoneServer and the controller logic is a per-tick machine over live AiAgent/squad state + pvpSquadMutex; no SimPvP gtest scaffold and a squad+enemy fixture dwarfs the assertions (mock-pain tripwire) | live-dashboard verification per `docs/ai-pvp-squad-design.md` §P.6.6b (`squadAggroSharing=true` restart: `combatEngagement.convergencesTotal` climbs when one member engages; squadmates path in + attack the same enemy with no clipping; a dying/leaving shared target releases the squad; no member idle-heals while a squadmate fights; far squadmates converge without timing out at the converge timeout)
- `PathFinderManager::findPathFromCellToWorld` F_0.8.0 egress repair ladder (interior exit-node re-hint from the agent's own cell position, source-cell-linked exterior global-node selection, reversed entry-path fallback) and `SimPlayerController` structure-traversal state machine (`enterStructure`/`exitStructure`, phase transitions, combat pause/resume with the compound peace predicate + traversal-generation-tagged resume monitor, `isWithinOwningBuildingHollow` containment, teleport/z-sanity watchdogs) | the pathfinder retries need a loadable POB portal-layout/floor-mesh fixture (an asymmetric hospital plus a multi-door starport) that no gtest scaffold provides, and the state machine is a per-tick machine over live `AiAgent`/`CellObject`/`BuildingObject` + task-scheduler timing where a fixture dwarfs the assertions (mock-pain tripwire) | the in-tree 25-scenario harness IS the test deliverable: enable `structureTraversal` + `structureTraversalTest` in `sim_player_manager.lua`, restart, and assert `structureTraversal.scenarios[]` all PASS with `anomalyCounters` at 0 and no `exitpath is nullptr` for hospitals in `log/pathfinder.log`, per `docs/ai-cell-navigation-design.md`. ESCAPE PLAN: the node-selection half of the repair ladder is pure graph math — extract it into a header-only helper taking `PathGraph`/`FloorMesh` pointers so it links into `core3tests` once a portal-layout fixture exists.
- `SimPlayerController::transformFromStructureModelSpace` (F_0.8.0 D1) and its callers `resolveHollowEscalationTarget` / `beginHollowEscalation` / `isWithinOwningBuildingHollowAt` | the transform is genuinely pure math and *worth* testing — a Y/Z axis swap in the first implementation was caught only by hand-deriving the matrix, exactly the class of bug a round-trip test kills instantly — but it is a file-local `static` taking a live `BuildingObject*` for position/direction, and the escalation callers additionally need a POB portal-layout fixture plus `PlanetManager` travel points; a building+portal fixture dwarfs the assertions (mock-pain tripwire) | ESCAPE PLAN (cheap, do this next slice): re-express the transform as a free function over plain values (`modelPoint`, building x/y/z, direction radians) in a header-only helper so `core3tests` can assert the round-trip identity `fromModelSpace(toModelSpace(p)) == p` over randomized positions/rotations with no fixture at all. Until then: live verification via the 25-scenario harness — `structureTraversal.anomalyCounters.hollowEscalationsTriggered > 0` with `hollowEscalationsFailed == 0`, `ST_EGRESS escalation=target source=exterior_node` chosen points landing outside the building AABB, and `teleportsDetected`/`zSanityViolations` staying 0 (a swapped axis would surface as a z-sanity violation or a no_candidate storm).
- `SimPlayerController::probeDirectOverlandClearance` (F_0.8.0 D7 Phase 1 segment-clearance probe) and the POB exit-set path `buildCellEgressExitSet` / `startNextCellEgressCandidate` / `failCellEgress` | the probe needs a live `Zone` with in-range collidable objects carrying real appearance meshes (the narrow phase is `CollisionManager::getAppearanceIntersection` against TRE-loaded geometry), and the exit-set builder needs a loadable POB `PortalLayout`/`PathGraph` with `PathNode::BuildingEntrance` nodes plus cell adjacency — no gtest scaffold provides either, and both fixtures dwarf the assertions (mock-pain tripwire). The per-candidate budget walk (`egressCandidateAttemptCap` per candidate under `egressTotalAttemptCeiling`, advance-on-exhaust, single exhaustion record) and the distance-from-bot insertion sort ARE pure-worthy, but both read the `SimPlayerManager` singleton for config and mutate controller state | live verification via the 25-scenario harness with `structureTraversal.zeroClip.logging=true`: Phase 1 asserts `ST_CLEARANCE` lines appear with a plausible `would_block` rate and `elapsedUs` cost, and that miner steady state is UNCHANGED (`enforce=false` must be behaviourally inert — `minerActivity.pathFailures` and stationed/moving counts match the pre-change baseline). Exit set asserts `zeroClip.exitSetsBuilt > 0`, `exitCandidatesTried > 1` on a failing door, and that a bot whose first exit fails is NOT left in the cell. ESCAPE PLAN (cheap, do with the D1 transform extraction already queued above): lift the candidate-budget walk and the insertion sort into a header-only helper over plain values (a `Vector<Vector3>` of candidates, an agent position, and the two int caps) so `core3tests` can assert ordering and budget exhaustion with no fixture at all — that covers the anti-stuck logic the owner specifically asked for, which is the part worth protecting.
- `SimPlayerController::observeHollowRadialScan` (F_0.8.0 D8 Phase 1 radial hollow scan): the 360-degree ray sweep, the escape test (`getAppearanceIntersection` over a single hoisted `getInRangeObjects` disc plus the bounding-sphere cull), and the CIRCULAR opening clustering (contiguous escaping bearings, wrap across index 0, `minOpeningDeg` rejection, all-escaped and none-escaped special cases) | the escape test needs a live `Zone` populated with the real collidable geometry that walls a starport landing pad — the whole point is that the enclosure is surrounding scenery rather than the starport's own portal layout, so no template fixture reproduces it, and a fixture that did would dwarf the assertions (mock-pain tripwire); no gtest scaffold covers SimPlayerController | live verification per `docs/1-plans/F_0.8.0-D8_hollow-radial-exit.proposal.md` Phase 1 acceptance: `ST_HOLLOW scan status=ok` with `openings` reported for every hollow-stuck bot, the same opening count and bearings observed on at least two different hollow starports, `elapsedUs` p95 comparable to the existing zeroClip clearance probe, and zero movement change (the scan is additive — escalation outcome handling is unchanged). ESCAPE PLAN (cheap, worth doing before Phase 2 moves any bot): the clustering is pure integer/angle math over a `std::vector<int>` of escape flags — lift it into a header-only helper taking (flags, raysCount, minOpeningDeg) and returning centre/width pairs, so `core3tests` can assert the wrap-at-zero, single-bearing, all-escaped, none-escaped and sub-threshold cases with no fixture at all. That covers the half of D8 most likely to be subtly wrong, independently of the geometry half which genuinely needs the live world.
